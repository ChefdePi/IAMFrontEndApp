<header>
  <div class="container">
    <h1><a href="/"><i class="fas fa-lock"></i> SecureIAM</a></h1>
    <nav>
      <a href="/">Home</a>
      <a href="/about">About</a>
      <a href="/contact">Contact</a>
      <% if (user) { %>
        <a href="/dashboard">Dashboard</a>
        <a href="/profile">My Profile</a>
        <% if (user.role==='Admin') { %><a href="/admin/users">Admin</a><% } %>
        <% if (['Editor','Admin'].includes(user.role)) { %><a href="/posts">Posts</a><% } %>
        <% if (['Viewer','Admin'].includes(user.role)) { %><a href="/reports">Reports</a><% } %>
        <a href="/logout">Logout</a>
      <% } else { %>
        <a href="/login">Login</a>
      <% } %>
    </nav>
  </div>
</header>
