<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Complete Your Profile</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>

  <header>
    <div class="container">
      <h1><i class="fas fa-lock"></i> SecureIAM</h1>
      <nav>
        <a href="/">Home</a>
        <a href="/dashboard">Dashboard</a>
        <a href="/logout">Logout</a>
      </nav>
    </div>
  </header>

  <section class="main-content">
    <div class="container">
      <h2>Complete Your Profile</h2>

      <!-- show the email coming in from req.user -->
      <p>
        <label>Email Address:<br>
          <input type="email" value="<%= email %>" readonly>
        </label>
      </p>

      <!-- any validation error -->
      <% if (error) { %>
        <p class="error"><%= error %></p>
      <% } %>

      <!-- profile form -->
      <form method="POST" action="/complete-profile">
        <p>
          <label>First Name:<br>
            <input name="firstName" value="<%= firstName||'' %>" required>
          </label>
        </p>

        <p>
          <label>Last Name:<br>
            <input name="lastName" value="<%= lastName||'' %>" required>
          </label>
        </p>

        <p>
          <label>Role:<br>
            <select name="role" required>
              <option value="">-- Select Role --</option>
              <option value="Caregiver"   <%= role==='Caregiver'   ? 'selected' : '' %>>
                Caregiver
              </option>
              <option value="FamilyMember" <%= role==='FamilyMember' ? 'selected' : '' %>>
                Family Member
              </option>
            </select>
          </label>
        </p>

        <button type="submit">Save Profile</button>
      </form>
    </div>
  </section>

  <footer>
    <div class="container">
      <p>&copy; 2025 SecureIAM. All rights reserved.</p>
    </div>
  </footer>

</body>
</html>
